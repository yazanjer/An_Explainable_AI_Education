# Core dependencies. Everything in src/vlpso_xai and tests/ runs with these
# alone. Installed first and separately so that a failure in an optional
# package (skrebate, Boruta, shap, lime) cannot abort the core install --
# which is exactly what an unsatisfiable skrebate constraint once did,
# silently leaving pyreadstat uninstalled.
numpy>=2.0,<3
pandas>=2.0,<3
scipy>=1.13,<2
scikit-learn>=1.4,<2
pyyaml>=6.0
pyarrow>=14.0
pyreadstat>=1.2
matplotlib>=3.7
tqdm>=4.66
joblib>=1.3
pytest>=8.0
